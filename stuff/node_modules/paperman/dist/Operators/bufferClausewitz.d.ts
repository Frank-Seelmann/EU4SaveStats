/// <reference types="node" />
import { Observable } from 'rxjs';
import { ClausewitzObject } from '../ClausewitzObject';
import { GameInfo } from '../GameInfo';
/**
 * ReactiveX Operator for turning binary data into ClausewitzObjects
 * @param gameInfo specifies what game this save is from
 */
export declare function bufferClausewitz<T extends Buffer, R extends ClausewitzObject>(gameInfo: GameInfo): (source: Observable<T>) => Observable<R>;
export default bufferClausewitz;
