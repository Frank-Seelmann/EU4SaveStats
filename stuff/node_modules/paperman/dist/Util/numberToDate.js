"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.numberToDate = void 0;
var zeroDate = 43800000;
/**
 * Turns a number representing a Clausewitz date into a string representation of that date
 */
var numberToDate = function (n) {
    if (n < zeroDate) {
        // negative dates are usually not printed as such, but there is one special case
        if (n === 43791240) {
            return '-1.1.1';
        }
        // in all other instances, just return the input number
        return n.toString();
    }
    n -= zeroDate;
    n /= 24;
    var year = 0;
    var month = 1;
    var day = 1;
    for (var i = 365; i <= n; i += 365) {
        year += 1;
    }
    n -= year * 365;
    [31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30].some(function (days) {
        if (n < days) {
            return true;
        }
        n -= days;
        month += 1;
        return false;
    });
    day += n;
    return year + "." + month + "." + day;
};
exports.numberToDate = numberToDate;
exports.default = exports.numberToDate;
